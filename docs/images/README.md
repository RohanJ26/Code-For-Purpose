# Images for README

PNG files in this folder are **linked from the root [`README.md`](../../README.md)** using paths like `docs/images/yourfile.png` (relative to the repo root). Replace them when the UI changes so judges always see current screens.

**Images not showing on GitHub?** The PNGs must be **committed and pushed** with the rest of the project. If this folder only contains `.svg` placeholders, re-add the screenshots from your machine (see team notes) or run the copy step from your saved assets.

## Current assets

| File | Content |
|------|---------|
| `flow-architecture.png` | **Combined** user flow + technical architecture (single diagram). |
| `screen-01-compare-datasets.png` | Compare datasets modal (Dataset A vs B, schema alignment). |
| `screen-02-analytics-scatter-insights.png` | Analytics — scatter chart (e.g. Age vs BMI) + AI insight cards. |
| `screen-03-dashboard-kpis.png` | Dashboard — KPI cards and suggested questions. |
| `screen-04-dashboard-chat.png` | Dashboard — Analysis chat with AI reply. |
| `screen-05-trends.png` | Trends page — trend cards and AI predictions. |
| `screen-06-formal-html-report.png` | Formal **HTML report** (executive summary + data dictionary). |
| `screen-07-analytics-charts.png` | Analytics — key metrics row + line/area charts. |

## Optional placeholders

- `flow-architecture.placeholder.svg` — only used if you remove the PNG and need a temporary graphic.
- `screen-placeholder.svg` — generic placeholder for new shots.

## Tips

- **Width:** README uses `width="92%"` for screenshots; export at **~1400–1600px** wide for sharp GitHub rendering.
- **Privacy:** Redact API keys, emails, and patient identifiers if using real data.
- **Format:** PNG is preferred; keep files under a few MB each if possible.
